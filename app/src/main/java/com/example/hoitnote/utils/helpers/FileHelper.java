package com.example.hoitnote.utils.helpers;

import android.graphics.Bitmap;

import com.example.hoitnote.utils.Interfaces.IFile;

/*
* 该类用于实现写图像到文件的功能
* */
public class FileHelper implements IFile {
    @Override
    public boolean saveBitmap(Bitmap bitmap, String fileName) {
        return false;
    }
}
